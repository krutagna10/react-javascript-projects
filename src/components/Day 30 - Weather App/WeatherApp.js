const WeatherApp = () => {
  return (
    <div>
      <h1>Weather App</h1>
      <form className="form">
        <input type="text" placeholder="Search" />
        <button>Search</button>
      </form>
    </div>
  );
};

export default WeatherApp;
